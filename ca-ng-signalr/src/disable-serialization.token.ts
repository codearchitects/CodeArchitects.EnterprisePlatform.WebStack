import { InjectionToken } from '@angular/core';

/**
 * If true, disables platform serialization, in favour of default JSON serialization
 */
export const DISABLE_SERIALIZATION = new InjectionToken<string>('DISABLE_SERIALIZATION');
