// Storybook provides its own application bootstrap. This file only exists to
// satisfy the Angular browser builder's `main` requirement for the build
// context (tsconfig, styles, scripts) that Storybook reuses.
export {};
